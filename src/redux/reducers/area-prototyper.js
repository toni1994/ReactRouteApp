const initialState = {
    folders: [{
        id: 0,
        name: "Bar",
        numProtoypes: 4
    },
    {
        id: 1,
        name: "Casino",
        numProtoypes: 2
    }],
}

export default function reducer(state = initialState , action){
    return state;
}